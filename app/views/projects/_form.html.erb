<%= simple_form_for @project do |f| %>
    <%= f.error_notification %>
    <%= f.input :name, placeholder: 'name' %>
    <%= f.text_area :content %>
    <%= f.button :submit, 'Submit Project' %>
<% end %>